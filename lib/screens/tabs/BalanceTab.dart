import 'package:flutter/material.dart';

class BalanceTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Balance Tab'),
    );
  }
}
